This is the folder where you can create your vendor list manager.

Add/Delete vendors from vendor list(s)
From API perspective
Input: Auth, vendor list name (default master list), vendors, account

Add/Delete Vendor lists
